<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 0.10.8: Paket-Sichtbarkeit für den MirMirStack-Installed-Check
         (Android 11+; ohne <queries> wirft getPackageInfo NameNotFoundException,
         der Menü-Eintrag würde nie erscheinen. Keine Permission.) -->
    <queries>
        <package android:name="com.heddrich.companion" />
    </queries>

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- 0.10.6: CPU-WakeLock für Screen-off-Aufnahmen (Normal-Permission, keine Runtime-Abfrage) -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" tools:ignore="PermissionImpliesUnsupportedChromeOsHardware" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- 0.11.2: Quick Settings Tile (TileService binding) -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <uses-feature android:name="android.hardware.microphone" android:required="true" />

    <application
        android:name=".SherpaTranscriptApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.SherpaTranscript"
        android:usesCleartextTraffic="true">

        <service
            android:name=".service.RecordingService"
            android:exported="false"
            android:foregroundServiceType="microphone" />

        <!-- Phase 8 (0.7.4): Aktionen der Aufnahme-Benachrichtigung (Stop / Screen wach) -->
        <receiver
            android:name=".ui.live.RecordingActionReceiver"
            android:exported="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.SherpaTranscript.Splash"
            android:windowSoftInputMode="adjustResize|stateAlwaysHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Phase 9 (0.9.0): Geteilte Sprachnachrichten empfangen (WhatsApp/Telegram/…) -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="audio/*" />
            </intent-filter>
        </activity>

        <!-- Phase 4 (0.6.2): Export-Dateien teilen (ShareSheet) -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- 0.11.2: Quick Settings Tile – sofortige Transkription -->
        <service
            android:name=".service.QuickStartTileService"
            android:exported="true"
            android:label="@string/tile_label"
            android:icon="@mipmap/ic_launcher"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>
    </application>
</manifest>
